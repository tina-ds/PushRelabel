# PushRelabel: исследование эвристик и экспериментальный анализ

Этот репозиторий содержит реализацию алгоритма **PushRelabel** для поиска максимального потока, а также экспериментальное исследование трёх классических эвристик:

- **Global relabeling**
- **Gap relabeling**
- **Highest-label selection rule**

Дополнительно рассматривается техническая оптимизация **current arc heuristic**, не являющаяся эвристикой порядка Push/Relabel, но широко используемая в практических реализациях.

## 📄 Содержание репозитория

- `PushRelabel.ipynb` — основной Jupyter-ноутбук, содержащий:
  - реализацию базового PushRelabel;
  - поэтапное добавление эвристик Global, Gap и Highest-label;
  - генерацию графов различных типов;
  - замеры производительности;
  - визуализацию результатов;
  - эксперимент с current arc heuristic;
  - общий аналитический вывод.

- `src/` (опционально) — модульные реализации классов PushRelabel.

- `README.md` — описание проекта (этот файл).

---

## 🧠 Описание задачи

В рамках задания требовалось:

1. Реализовать алгоритм **PushRelabel**.
2. Добавить и сравнить не менее двух эвристик:
   - Global relabeling (обязательно)
   - Gap relabeling (обязательно)
   - Дополнительно: Highest-label
3. Провести замеры времени работы на трёх типах графов:
   - линейные цепочки,
   - многослойные графы,
   - случайные ориентированные графы с гарантированной достижимостью `s → t`.
4. Построить графики, сравнить результаты.
5. Придумать свой граф, на котором конкретная эвристика работает особенно хорошо или плохо.
6. Дополнительно: исследовать влияние оптимизации *current arc heuristic*.

---

## 🔧 Реализованные варианты алгоритма

| Вариант                    | Описание |
|----------------------------|----------|
| `base`                     | FIFO-очередь активных вершин, без эвристик |
| `global`                   | + Global relabeling |
| `global_gap`               | + Global relabeling + Gap relabeling |
| `highest`                  | Highest-label selection rule |
| `base_current`             | base + current arc heuristic |

---

## 🔬 Типы исследованных графов

1. **Линейные пути (`s → v₁ → ... → t`)**  
   — малое число рёбер, низкая сложность структуры.

2. **Многослойные графы (полносвязные слои)**  
   — плотные сети, много параллельных путей.

3. **Случайные s–t-графы**  
   — достигаемость обеспечена скелетным путём; плотность задаётся параметром `p`.

4. **Специально сгенерированный граф**  
   — структура, на которой Highest-label работает плохо, а Gap/Global — хорошо.

---

## 📊 Краткие результаты

### ✔ Основной победитель  
**Global relabeling + Gap relabeling** оказалась наиболее быстрой, стабильно доминируя на всех типах графов:

- на линейных путях — самое быстрое решение,
- на многослойных графах — ускорение в десятки раз,
- на случайных графах — превосходство в сотни и тысячи раз.

### ✔ FIFO без эвристик  
Показывает "взрыв" времени на плотных графах (в сотни раз медленнее эвристик).

### ✔ Highest-label  
Работает нестабильно:  
прекрасно на некоторых типах графов, резко деградирует на других (особенно — многослойных).

### ✔ current arc heuristic (бонус)  
Техническая оптимизация.  
Не относится к эвристикам Push/Relabel.  
Ускоряет алгоритм не всегда — только на специфических структурах.

---

## 📈 Визуализация

В ноутбуке содержатся графики:

- времени работы на линейных графах,
- времени работы на многослойных графах,
- времени работы на случайных графах (с логарифмической шкалой),
- сравнение на специально построенном графе,
- влияние current arc heuristic.

---

## 🏁 Итоговый вывод

Алгоритм PushRelabel **критически зависит** от порядка выполнения операций.  
Эвристики *Global relabeling* и *Gap relabeling* обеспечивают наилучшее соотношение скорости и стабильности, будучи устойчивыми на линейных, многослойных и случайных графах.

Все реализации дают **одинаковый максимальный поток**, что подтверждает корректность экспериментов.

---

## 🚀 Как запустить

```bash
git clone <repo-url>
cd <repo-folder>
jupyter notebook PushRelabel.ipynb
